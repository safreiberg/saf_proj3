<h1><%= @post.title %></h1>

<h2><%= @post.content %></h2>

<h3>Comments</h3>

<% if session[:authenticated] %>

<%= form_tag '/posts/add_comment', :remote => true, :class => "addcomment" do %>
  <%= label_tag(:content, "Comment:") %>
  <%= text_area_tag(:content) %>
  <%= hidden_field_tag 'user_id', session[:user_id] %>
  <%= hidden_field_tag 'post_id', params[:id] %> <br />
  <%= submit_tag("Submit") %>
<% end %>

<% end %>

<div id="commentstable" border=1>
</div>

<script type="text/javascript">
update_comments();
setInterval( update_comments, 8000 );

$('document').ready(function() {
  $(".addcomment").live("ajax:send", function(event,xhr,status){
    $('#content').val('');
    update_comments();
  });
  $("a").live("ajax:send", function(event,xhr,status){
    update_comments();
  });
});


</script>